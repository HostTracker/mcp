{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//mcp": "No secrets live here: the caller supplies their own HostTracker API token per request. api2BaseUrl = the HostTracker API root the tools call (override with the environment variable mcp__api2BaseUrl); api2TimeoutMs = per-call upstream timeout; rateLimitPerMinute = per-IP cap on the MCP endpoint.",
  "mcp": {
    "api2BaseUrl": "https://api2.host-tracker.com",
    "api2TimeoutMs": 20000,
    "rateLimitPerMinute": 60
  }
}
