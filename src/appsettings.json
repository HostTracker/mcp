{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//mcp": "No secrets live here: the caller supplies their own HostTracker API token per request. api2BaseUrl = the HostTracker API root the tools call (override with the environment variable mcp__api2BaseUrl); api2TimeoutMs = per-call upstream timeout; rateLimitPerMinute = per-IP cap on the MCP endpoint; oauthIssuer/publicBase = the OAuth discovery switch - set oauthIssuer to your OAuth authorization server origin to enable the 401 challenge + RFC 9728 protected-resource metadata, leave it empty for plain bearer-token use.",
  "mcp": {
    "api2BaseUrl": "https://api2.host-tracker.com",
    "api2TimeoutMs": 20000,
    "rateLimitPerMinute": 60,
    "oauthIssuer": "",
    "publicBase": "https://mcp.host-tracker.com"
  }
}
